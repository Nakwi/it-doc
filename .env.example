# IT-DOC Environment Variables
# Copy this file to .env and adjust values as needed

# Development Server Configuration
# Default: localhost (secure by default)
# Set to your IP (e.g., 192.168.1.11) only if you need LAN access
DEV_HOST=localhost
DEV_PORT=4321
